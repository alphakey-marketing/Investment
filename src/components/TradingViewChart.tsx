/**
 * TradingViewChart.tsx — DEPRECATED STUB
 *
 * TradingView free embed widget does not have data rights for HKEX securities
 * (shows "此商品僅tradingview上可用"). Component kept as empty stub to avoid
 * import errors in case it is referenced elsewhere.
 *
 * The real chart is now KlineChart.tsx fed by Yahoo Finance data
 * via the server-side /api/yahoo-klines route.
 */
export default function TradingViewChart() {
  return null;
}
